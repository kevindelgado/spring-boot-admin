server:
    port: 8080
spring:
    application:
        name: spring-boot-admin-server-test
    jmx:
        enabled: false

management:
  info:
    env:
      enabled: true
logging:
  level:
    de.codecentric: DEBUG
